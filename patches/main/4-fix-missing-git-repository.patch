diff --git a/lib/git_utils.rb b/lib/git_utils.rb
index ff00cec886a..5ca84a86da7 100644
--- a/lib/git_utils.rb
+++ b/lib/git_utils.rb
@@ -12,7 +12,7 @@ class GitUtils
 
   def self.full_version
     filesystem_overrides["full_version"] ||
-      try_git('git describe --dirty --match "v[0-9]*" 2> /dev/null', "unknown")
+      try_git('git describe --dirty --match "v[0-9]*" 2> /dev/null', Discourse::VERSION::STRING)
   end
 
   def self.has_commit?(hash)
@@ -23,7 +23,8 @@ class GitUtils
 
   def self.last_commit_date
     git_cmd = 'git log -1 --format="%ct"'
-    seconds = try_git(git_cmd, nil)
+    # Note(decentral1se): Output from actual command in the v2026.7.1 branch
+    seconds = self.try_git(git_cmd, '1721046633')
     seconds.nil? ? nil : DateTime.strptime(seconds, "%s")
   end
 
